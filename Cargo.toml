[package]
name = "RustStack"
version = "0.1.0"
edition = "2024"

[dependencies]
qubit = "0.10.1"

ts-rs = "10.1.0"
serde = { version = "1.0", features = ["derive"] }
futures = "0.3.31"

tokio = { version = "1.44", features = ["full"] }
axum = "0.8"
hyper = { version = "1.6", features = ["server"] }